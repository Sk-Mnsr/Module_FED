; Limites upload pour le serveur intégré Laravel (php artisan serve / npm run dev:all)
; Doit être >= OD_MAX_UPLOAD_MB dans .env (défaut 25 Mo)
upload_max_filesize = 32M
post_max_size = 64M
max_file_uploads = 30
